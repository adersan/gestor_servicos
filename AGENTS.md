# AGENTS.md - Gestor de Servicos

Este arquivo orienta qualquer agente/Codex que continuar o projeto. O sistema ja esta em uso real, entao mudancas devem ser pequenas, testadas localmente e publicadas em lote para economizar creditos do Netlify.

## Local Atual Do Projeto

- Projeto principal: `D:\GitHub\gestor_servicos`
- Site em producao: `https://gestordeservicos.com.br`
- Site Netlify original: `https://gestor-servicos-adersan.netlify.app`
- Banco: Supabase do projeto `tfkhhxopxupbefaaijcz`
- Branch principal: `main`

## Estado De Publicacao

- O Netlify faz deploy automaticamente quando existe `git push` para o GitHub.
- Nao publicar pequenas alteracoes isoladas sem autorizacao do usuario.
- Preferencia atual: desenvolver e testar localmente, commitar local se fizer sentido, e so dar `git push` quando o usuario autorizar um pacote.
- O projeto ja consumiu muitos creditos do Netlify por deploys frequentes. Economizar deploys e uma regra importante.

## Estrutura Principal

- `index.html`: painel administrativo principal.
- `app.js`: logica principal do admin, renderizacao das telas, lancamentos, clientes, financeiro, fornecedores e dashboard.
- `styles.css`: estilos globais do painel administrativo.
- `data.js`: camada de dados local/Supabase, sincronizacao, upsert/delete e cache local.
- `auth.js`: autenticacao administrativa com Supabase.
- `config.js`: configuracao local real com URL e anon key do Supabase. Nao commitar segredos.
- `config.example.js`: modelo seguro de configuracao.
- `server.js`: servidor local simples para desenvolvimento.
- `sw.js`: service worker/cache PWA. Atualizar versao quando publicar alteracoes de frontend. Tem tambem listeners `push`/`notificationclick` para notificacoes push (ver secao Netlify Functions).
- `manifest.webmanifest`: manifesto PWA.
- `logo.svg`, `icon-192.png`, `icon-512.png`: identidade visual/PWA.
- `package.json`/`package-lock.json`: primeira dependencia npm do projeto (`web-push`, usada pelas Netlify Functions de notificacao push). Rodar `npm install` apos clonar/puxar antes de publicar.

## Portais Publicos

- `cliente.html`, `cliente.js`, `cliente.css`: portal de cobranca do cliente com credenciais/link.
- `acompanhamento.html`, `acompanhamento.js`, `acompanhamento.css`: acompanhamento de servicos pelo cliente, somente leitura e pedidos online quando permitido.
- `fornecedor.html`, `fornecedor.js`, `fornecedor.css`: portal do fornecedor, acompanhamento/confirmacao conforme permissoes do link.

## Netlify Functions

As funcoes ficam em `netlify/functions`.

- `client-login.mjs`: login temporario do cliente.
- `client-magic-login.mjs`: acesso do cliente por link assinado.
- `issue-client-access.mjs`: gera acesso/credenciais de cobranca.
- `issue-client-magic-link.mjs`: gera link direto de cobranca.
- `issue-service-tracking-link.mjs`: gera link de acompanhamento do cliente.
- `service-tracking-data.mjs`: entrega dados do acompanhamento.
- `client-service-request.mjs`: recebe pedido online do cliente (portal de cobranca `cliente.html`, autenticado via `verifyPortalToken`).
- `tracking-service-request.mjs`: recebe pedido online do cliente pelo link de acompanhamento (`acompanhamento.html`, autenticado por `accessCodeHash` do link) — e o endpoint realmente usado pelo modal "Novo pedido" do acompanhamento; nao confundir com `client-service-request.mjs`, sao dois portais/fluxos diferentes que gravam na mesma tabela.
- `admin-client-service-requests.mjs`: consulta/gestao administrativa de pedidos online.
- `issue-supplier-link.mjs`: gera link do fornecedor.
- `supplier-portal-data.mjs`: entrega dados ao portal do fornecedor.
- `supplier-portal-save.mjs`: salva alteracoes permitidas pelo fornecedor.
- `payment-webhook.mjs`: webhook de baixa automatica de pagamentos.
- `keep-alive.mjs`: manter vivo/verificacao leve.
- `apibrasil-whatsapp-*.mjs` e `whatsapp-status-webhook.mjs`: integracao experimental com APIBrasil/WhatsApp.
- `push-subscribe.mjs` / `push-unsubscribe.mjs`: salvam/removem a inscricao de notificacao push de um aparelho/admin (autenticado via `requireAdmin`).
- `push-overdue-check.mjs`: funcao agendada (`export const config = { schedule: "*/30 * * * *" }`, roda a cada 30 min) que reproduz a regra de atraso do painel (servico "A fazer" ha 24h+, cobranca com saldo aberto e periodo vencido) e dispara push uma unica vez por item (controle em `push_notified_alerts`).
- `_shared/server.mjs`: helpers compartilhados de servidor/Supabase.
- `_shared/apibrasil-whatsapp.mjs`: cliente compartilhado APIBrasil.
- `_shared/push.mjs`: `sendPushToAllAdmins(payload)`, usa a biblioteca `web-push` (VAPID) para mandar notificacao a todas as inscricoes salvas; remove automaticamente inscricoes que voltam 404/410 (expiradas). Chamado por `client-service-request.mjs`, `tracking-service-request.mjs` (pedido novo, imediato) e `push-overdue-check.mjs` (atrasos, periodico).

## Supabase

SQL e migracoes ficam em `supabase`.

- `schema.sql`: esquema base.
- `suppliers.sql`: fornecedores e servicos de fornecedor.
- `client_service_requests.sql`: pedidos online de clientes.
- `service_tracking_links.sql`: links de acompanhamento.
- `client_magic_link.sql`: links diretos de cliente.
- `service_cancellations.sql`: cancelamentos e motivos.
- `service_entry_groups.sql`: agrupamento de servico principal/complementar.
- `service_status_dates_and_supplier_whatsapp.sql`: datas de feito/entregue e campos WhatsApp fornecedor.
- `supplier_portal_permissions.sql`: permissoes do portal do fornecedor.
- `supplier_portal_show_entries.sql`: exibicao/lista para fornecedor.
- `whatsapp_sessions.sql`: sessoes WhatsApp.
- `push_subscriptions.sql`: tabela de inscricoes de notificacao push por admin/aparelho e tabela `push_notified_alerts` (controle de alerta de atraso ja avisado, evita repetir a cada checagem periodica).
- `fix_*.sql`: scripts corretivos pontuais.

Antes de alterar codigo que depende de coluna/tabela nova, criar ou atualizar o SQL correspondente e orientar o usuario a executar no Supabase.

## Variaveis De Ambiente

Variaveis secretas ficam no Netlify, nunca no frontend:

- `SUPABASE_URL`
- `SUPABASE_SECRET_KEY`
- `CLIENT_PORTAL_SECRET`
- `PAYMENT_WEBHOOK_SECRET`
- `WHATSAPP_WEBHOOK_SECRET`
- `APIBRASIL_DEVICE_TOKEN`
- `APIBRASIL_BEARER_TOKEN`
- `APIBRASIL_WHATSAPP_SESSION`
- `APIBRASIL_BASE_URL` opcional
- `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`: notificacoes push (web-push). So `VAPID_PRIVATE_KEY` e secreta de verdade; ja cadastradas no Netlify.

No navegador usar somente `supabaseAnonKey`/`supabasePublishableKey` e `vapidPublicKey` em `config.js` — `vapidPublicKey` nao e segredo (e enviado ao navegador por design, faz parte do protocolo Web Push), so a chave privada correspondente e que fica exclusivamente no Netlify.

## Como Rodar Localmente

Na pasta `D:\GitHub\gestor_servicos`:

```powershell
node server.js
```

Abrir:

```text
http://localhost:8080
```

Tambem pode ser usado:

```powershell
python -m http.server 8094 --directory D:\GitHub\gestor_servicos
```

## Testes E Validacao

Existe `package.json` (so pra dependencia `web-push` das Netlify Functions de push) — rodar `npm install` uma vez apos clonar/puxar. Os testes atuais continuam sendo scripts Node diretos, sem framework.

Comandos recomendados antes de commit/deploy:

```powershell
node --check app.js
node --check data.js
node --check cliente.js
node --check acompanhamento.js
node --check fornecedor.js
node --check supplier.js
node tests\billing-rollover.test.mjs
node tests\reference-history.test.mjs
git diff --check
```

Se a alteracao mexer em Netlify Functions, validar tambem:

```powershell
node --check netlify\functions\nome-da-funcao.mjs
```

## Publicacao E Cache

Quando for publicar frontend:

1. Atualizar query strings em `index.html` se `app.js`, `styles.css` ou outros assets mudarem.
2. Atualizar `sw.js`:
   - nome/numero do cache;
   - lista de assets com as novas query strings.
3. Rodar validacoes.
4. Commitar.
5. So dar `git push origin main` com autorizacao do usuario.
6. Conferir producao depois do deploy.

Sem versionamento de cache, o celular pode continuar usando arquivo antigo.

## Regras De Trabalho

- Nao reverter alteracoes que nao foram feitas por voce.
- Conferir `git status -sb` antes de editar/commitar.
- Manter alteracoes pequenas e coerentes com o padrao atual.
- Evitar refatoracoes grandes enquanto o sistema esta em uso.
- Usar `apply_patch` para edicoes manuais.
- Nao gravar tokens, bearer, service role ou chaves secretas em arquivos versionados.
- Se mudar banco, documentar qual SQL deve ser executado no Supabase.
- Se a mudanca impactar celular, testar mentalmente layout responsivo e reduzir excesso visual.
- Se a mudanca impactar financeiro/cobranca, conferir pagamentos parciais, quitacao, credito e saldo anterior.

## Regras Para Codex No Terminal

Estas regras refletem o combinado com o usuario durante o desenvolvimento real do sistema.

- Trabalhar sempre a partir da pasta `D:\GitHub\gestor_servicos`.
- Antes de qualquer alteracao, rodar `git status -sb` e identificar arquivos ja modificados.
- Nao reverter, apagar ou sobrescrever mudancas existentes que nao foram feitas por voce.
- A alteracao atual em `.gitignore` nao pertence ao agente; nao incluir em commits e nao reverter sem pedido explicito.
- Fazer mudancas localmente primeiro. O sistema esta em uso real.
- Commits locais sao permitidos quando organizam bem o trabalho.
- Nao executar `git push` sem autorizacao clara do usuario.
- Quando o usuario autorizar "pode subir", publicar o lote ja commitado/validado sem pedir nova confirmacao; ainda assim, sempre perguntar antes de qualquer `git push` quando nao houver autorizacao explicita no turno.
- Cada `git push origin main` dispara deploy automatico no Netlify e consome creditos.
- Agrupar pequenas correcoes em lote antes de publicar.
- Antes de publicar frontend, atualizar versoes de cache em `index.html`, `sw.js` e no registro do service worker em `app.js` quando houver mudanca em assets cacheados.
- Depois de mudar `app.js`, normalmente atualizar:
  - `index.html`: `app.js?v=N`
  - `sw.js`: `CACHE = "gestor-servicos-vN"` e asset `app.js?v=N`
  - `app.js`: `navigator.serviceWorker.register("sw.js?v=N")`
- Rodar validacoes antes de commit/deploy:
  - `node --check app.js`
  - `node --check data.js`
  - `node --check cliente.js`
  - `node --check acompanhamento.js`
  - `node --check fornecedor.js`
  - `node --check supplier.js`
  - `node tests\billing-rollover.test.mjs`
  - `node tests\reference-history.test.mjs`
  - `git diff --check`
- Se a mudanca envolver Netlify Functions, rodar `node --check` na funcao alterada.
- Se a mudanca exigir banco, criar/atualizar arquivo SQL em `supabase` e avisar o usuario exatamente qual script executar no Supabase.
- Nunca colocar secrets no codigo:
  - Supabase service role/secret key
  - Bearer token
  - Device token
  - Webhook secret
  - Chaves de API
- Secrets devem ficar somente nas variaveis de ambiente do Netlify.
- Evitar comandos destrutivos como `git reset --hard`, `git checkout --`, `rm`/`Remove-Item` sem pedido explicito e confirmacao.
- Preferir patches pequenos e revisar o diff antes de commitar.
- Ao final, informar:
  - arquivos alterados;
  - testes executados;
  - se foi publicado ou ficou apenas local;
  - se existe alguma pendencia de SQL/cache/deploy.

### Publicacao

Fluxo recomendado quando o usuario disser claramente "pode subir":

1. Rodar `git status -sb`.
2. Confirmar que apenas arquivos do lote serao commitados.
3. Atualizar cache se necessario.
4. Rodar testes e `git diff --check`.
5. Fazer commit com mensagem objetiva.
6. Executar `git push origin main`.
7. Aguardar o Netlify publicar.
8. Verificar `https://gestordeservicos.com.br` e confirmar se a versao nova apareceu.

### Git No Windows

Se `git` nao estiver no PATH no terminal, usar o Git do GitHub Desktop:

```powershell
C:\Users\aders\AppData\Local\GitHubDesktop\app-3.5.8\resources\app\git\cmd\git.exe
```

Exemplo:

```powershell
C:\Users\aders\AppData\Local\GitHubDesktop\app-3.5.8\resources\app\git\cmd\git.exe status -sb
```

## Fluxos Importantes Do Sistema

### Cliente

- Cadastro, tabelas, servicos, lancamentos e pedidos online ficam agrupados no menu `Clientes`.
- A ordem desejada do submenu de clientes e: `Lancamento`, `Pedido on-line`, `Cadastro`, `Servicos`.
- Lancamentos da tela de cliente devem priorizar a semana operacional atual, mas busca por referencia/placa deve poder localizar em todo o historico.
- Servicos complementares ficam vinculados ao servico principal. No resumo, complementares nao contam como servicos principais, mas seu valor aparece separado.

### Financeiro

- Pagamentos podem ocorrer antes ou depois da geracao da cobranca.
- Pagamentos ja abatidos/vinculados nao devem ficar editaveis.
- Cobrancas podem estar abertas, parciais, quitadas ou canceladas.
- Ao gerar cobranca, pagamentos do periodo devem abater a cobranca.
- Se pagamento passar do valor devido, tratar como credito para abater futuramente.
- O resumo financeiro deve evitar mostrar pagamento atrasado como credito indevido da semana atual.

### Fornecedor

- Fornecedores possuem cadastro, servicos, lancamentos, pagamentos, relatorios e portal proprio.
- Servico de fornecedor pode ser gerado a partir de lancamento do cliente ou lancamento direto.
- Status do fornecedor pode avancar automaticamente quando o servico do cliente vira `Feito` ou `Entregue`, mas pode ser ajustado manualmente.
- Ao excluir ou cancelar servico do cliente, perguntar o que fazer com fornecedores vinculados.
- Registrar datas de `Feito` e `Entregue` quando status muda.

### Portais

- Cliente e fornecedor veem apenas o que o link/token permite.
- Links devem ser somente leitura salvo permissoes explicitas, especialmente no fornecedor.
- Alteracoes feitas pelo fornecedor devem ficar marcadas para o administrador identificar.

## Observacoes Recentes

- Ha uma alteracao preexistente em `.gitignore`; nao reverter sem pedido explicito.
- Publicado: botao `Voltar para Feito`, busca por digitacao (datalist) em todos os modais com lista que podiam crescer, e correcao do rodape fixo do modal de lancamento no mobile. Regra adotada: todo modal com lista que pode crescer deve permitir digitar para buscar, sem excecao.
- Publicado: portal de acompanhamento (`acompanhamento.html`/`.js`/`.css`) virou uma area de 3 abas — Servicos, Financeiro (tempo real, mesmo sem cobranca gerada) e Relatorio de cobranca (so aparece quando existe cobranca do cliente). Helpers `billingOpenAmount`/`selectBillingPaymentMethods` extraidos para `_shared/server.mjs` e reaproveitados por `client-statement.mjs`. O `cliente.html`/`.js`/`.css` (login por cobranca) nao foi alterado.
- Publicado e testado pelo usuario em producao: link de acompanhamento com dois niveis de acesso. Sem senha: so aba Servicos, sem valores, mas pode fazer pedido. Com senha: acesso completo aos itens escolhidos na geracao do link (abas Financeiro/Relatorio de cobranca e quais servicos do catalogo ficam visiveis). Admin escolhe no `trackingDialog` entre "link com senha embutida" (URL com parametro `full=`, entra direto sem tela de escolha) ou "gerar identificador/senha para digitar" (com botoes de copiar) — so esse segundo modo mostra a tela de escolha de verdade. Tela de entrada mostra so os dois botoes ("Entrar sem senha"/"Entrar com senha"); a caixa de identificador/senha so aparece ao clicar em "Entrar com senha". Links gerados antes dessa mudanca continuam abrindo direto, sem tela de escolha (`linkMode` distingue "legacy" de "gated"). O backend zera os valores (`amount`) na resposta quando o acesso e restrito, em vez de confiar so na tela pra esconder (evita ver preco pelo DevTools). Novo helper `resolveTrackingTier` em `_shared/server.mjs`, testado em `tests/tracking-tier.test.mjs`. Novas colunas em `service_tracking_links` (`identifier_hash`, `password_hash`, `full_token_hash`, `full_show_financial`, `full_show_billing`, `visible_service_ids`) — SQL ja executado no Supabase pelo usuario.
- Para testar as Netlify Functions localmente (o `node server.js` so serve estatico), usar `netlify dev` (via `netlify-cli`, `netlify link` feito na pasta do projeto). Atencao: se a funcao reclamar de variavel de ambiente nao configurada mesmo com o site linkado, verificar no painel do Netlify (Site configuration > Environment variables) se cada variavel tem o escopo "Local development" habilitado — sem isso o `netlify dev` nao consegue ler as variaveis mesmo que `netlify env:list` mostre elas configuradas.
- Ainda nao publicado: modal de lancamento (`#serviceDialog`) ganhou um fluxo de perguntas por etapa no mobile/tablet, so para lancamento novo (inclusive "Importar pedido") — editar um lancamento existente continua abrindo o formulario completo de uma vez, como sempre. No desktop nada mudou. Cada etapa e um agrupamento do mesmo DOM/campos de sempre (`.wizard-step` em `index.html`), sem novo caminho de salvamento: os botoes Sim/Nao (solicitante, complementares, fornecedor), Hoje/Amanha/Outra data e os 3 botoes de Situacao so manipulam os inputs reais e disparam `change`, reaproveitando `toggleServiceRequesterSection`/`toggleAdditionalServices`/o listener de `hasSupplierService` em `supplier.js` que ja existiam. Nenhum botao de escolha avanca sozinho — sempre exige toque em "Continuar" (so o Sim revela campo novo foca nele; Nao/Hoje/Amanha/Situacao movem o foco pro Continuar, pra Enter do teclado fisico avancar direto). Etapa final mostra um resumo em cards (label+valor, borda e fundo clarinho) e aciona o mesmo `form.requestSubmit(button[value="default"])` do submit de sempre (`app.js`, controlador perto de `openEntryForm`, funcoes `setServiceWizardMode`/`goToServiceWizardStep`/`validateServiceWizardStep`/`renderServiceWizardSummary`/`syncServiceWizardChoiceSelection`). Botao X do `#serviceDialog` ganhou fechamento explicito (`closeServiceDialog`) por nao fechar de forma confiavel so com o `method="dialog"` nativo dentro do fluxo do wizard. Especificacao completa (medidas exatas de CSS, logica de cada etapa) documentada pelo usuario em `LANCAR-MOVEL.md` na raiz do projeto (nao versionado). Breakpoint mobile do `styles.css` (o unico bloco `@media`) e dos `matchMedia` em `app.js` foi ampliado de `700px` para `1024px` a pedido do usuario, pra cobrir tablets e celular deitado — testado e aprovado pelo usuario em tablet e celular deitado.
- Ainda nao publicado: corrigido um bug em que dialogs pequenos (ex.: `#deleteServiceDialog`) colapsavam no mobile/tablet (altura de um botao, conteudo sumindo) — a regra `dialog[open]:not(.wide-dialog)`/`dialog form` no `@media` de `styles.css` usava `max-height:100%` no form contra um `dialog` sem `height` explicita (so `max-height`), o que e ambiguo para resolucao de porcentagem; trocado para layout flexbox (`display:flex;flex-direction:column` no dialog, `flex:1 1 auto;min-height:0` no form), que resolve isso de forma robusta pra qualquer dialog, nao so o do wizard. Etapa Data do wizard: removido o botao "Amanha" (so Hoje/Outra data) e o campo de data nao abre mais o seletor nativo sozinho ao entrar na etapa — so quando o usuario toca "Outra data" ou toca no proprio campo (`firstVisibleServiceField` em `app.js` ignora `type=date` no auto-foco). Etapa Placa/referencia agora exige pelo menos uma referencia antes de avancar (Enter ou Continuar), mas Enter com o campo vazio ainda avanca se ja existir alguma na lista. Etapas Servico, Complementares e Fornecedor ganharam um seletor de botoes paginado, ordenado pelos itens mais usados no historico (`catalogUsagePickerItems` em `app.js`; `pickerSuppliers`/`pickerServicesForSupplier` novos em `window.supplierModule`) — tocar num botao ainda nao adicionado ja adiciona direto (usa preco sugerido/custo auto-preenchido); tocar num ja adicionado remove. Ver bullet mais recente abaixo pra contagem de passos/pagina atualizada (esses numeros mudaram depois desta nota).
- Ainda nao publicado: modal de lancamento do fornecedor foi dividido em duas etapas (Fornecedor = so nome, nova etapa Servico do fornecedor = so o servico+custo+lista) — total agora 10 passos, com pulo automatico da etapa "Servico do fornecedor" (avancar e voltar) quando a resposta for "Nao" (`goToServiceWizardStep` calcula direcao e pula o passo 8 se `hasSupplierService` desmarcado). Pickers (Servico, Complementares, Fornecedor, Servico do fornecedor) agora mostram 6 itens por pagina (`WIZARD_PICKER_PAGE_SIZE`) e o campo de digitar/buscar fica escondido por padrao (`wizard-picker-search-field hidden`, so existe em modo wizard — `setServiceWizardMode` reexibe esses campos quando o wizard esta desligado, senao quebraria o formulario desktop/edicao), revelado por um novo botao "Buscar" entre "Voltar" (renome de "Anterior") e "Mais" na barra de paginacao (`data-picker-search`), ou automaticamente quando a validacao falha (`revealPickerSearchField`). Atencao: como o proprio wizard-nav (barra fixa embaixo) tambem tem um botao "Voltar" (etapa anterior), ficam dois "Voltar" na tela ao mesmo tempo com significados diferentes — decisao do usuario, so documentando.
- Publicado em producao e concluido: rollout completo do modelo de etapas (wizard) do `#serviceDialog` para os demais modais do sistema, no mobile/tablet. Controlador reaproveitavel `createDialogWizard(config)` em `app.js` (navegacao entre etapas, barra de progresso, foco, botoes de escolha curta via `data-choice-for`/`data-choice-value`, picker de cards paginado via `config.pickers`) — sem tocar no codigo especifico ja existente do `serviceDialog` (nao foi migrado, zero risco pro que ja estava aprovado). Todos os demais dialogs de formulario sequencial foram convertidos e testados/aprovados pelo usuario: `paymentDialog`, `billingDialog`, `paymentMethodDialog`, `supplierDialog`, `supplierEntryDialog`, `supplierPayableDialog`, `supplierServiceDialog`, `billingBatchDialog`, `supplierPaymentDialog`, `catalogDialog`, `trackingDialog`, `supplierAccessDialog` (esses dois ultimos com painel de resultado condicional dentro da propria etapa de Confirmação, sem fechar o dialog sozinho). Ficam de fora, por decisao explicita: `clientRequesterDialog` e `whatsappDialog` (nao se encaixam no modelo "responde e fecha"), e `clientDialog` ("Novo cliente", 14 campos) — usuario decidiu manter as abas Principal/Cadastro em vez de converter para etapas.
- Publicado em producao e testado pelo usuario: botao flutuante `+` (`#newEntryFab`, classe `.new-entry-fab` em `index.html`/`styles.css`) fixo no canto inferior direito, visivel em TODAS as paginas (desktop e mobile/tablet) — abre o mesmo `serviceDialog` de sempre reaproveitando o atributo `data-dialog="serviceDialog"` ja tratado pelo listener delegado generico em `app.js`, sem JS novo. Tamanho 30% menor que o original (39px desktop, 36px mobile). Desktop: bem baixo (`bottom:28px`), proximo do toast (`#appToast`, tambem fixo no canto inferior direito) — pode encostar ocasionalmente quando o toast aparece, aceito pelo usuario. Mobile/tablet: colado na `.mobile-bottom-nav` (gap de 2px). Some quando o menu mobile esta aberto (`body.mobile-menu-open .new-entry-fab{display:none}`), igual a barra inferior.
- Publicado em producao e testado pelo usuario: modelo de etapas (wizard) levado tambem para os PORTAIS PUBLICOS (fora do escopo do rollout do admin, que so cobria `index.html`) — pedido do usuario pra ficar mais facil pro cliente e pro fornecedor usarem os modais que eles proprios acessam pelo link gerado. Como `acompanhamento.html`/`.js`/`.css` e `fornecedor.html`/`.js`/`.css` sao paginas totalmente separadas do admin (nao carregam `app.js`, sem acesso ao `createDialogWizard`), foi implementado um controlador de etapas proprio e mais simples em cada arquivo (sem sistema de picker de cards, sem `matchMedia` — aqui o wizard fica SEMPRE ativo, tanto desktop quanto mobile/tablet, ao contrario do admin que so ativa em telas pequenas):
  - `acompanhamento.html`: dialog `trackingRequestDialog` ("Novo pedido", pedido do cliente) virou 5 passos (Serviço+Valor / Placa-referência / Quem solicitou / Observação / Confirmação) — sempre foi só "criar", sem conceito de editar, entao sem modo dual. Funcoes novas em `acompanhamento.js`: `goToTrackingRequestStep`/`validateTrackingRequestStep`/`renderTrackingRequestWizardSummary`. Classes CSS novas em `acompanhamento.css`: `.tracking-wizard-*`.
  - `fornecedor.html`: dialog `entryDialog` ("Lançar serviço" do fornecedor) virou 7 passos (Serviço/Data/Placa-referência/Valor/Status-pula se sem permissao canMarkDone/Observações/Confirmação). Inicialmente so pra lançamento novo (editar mantinha formulario completo), mas depois de "editar tambem" (ver bullet abaixo) passou a usar wizard tambem pra editar — `activateEntryWizard(true)` chamado tanto no botao "Lançar serviço" quanto no `data-edit`. Funcoes em `fornecedor.js`: `activateEntryWizard`/`goToEntryStep`/`validateEntryStep`/`renderEntryWizardSummary`/`entryWizardShouldSkip`. Classes CSS em `fornecedor.css`: `.wizard-*` (mesmos nomes do admin, arquivo separado, sem conflito).
- Publicado em producao e testado pelo usuario: barra de navegacao inferior mobile (`.mobile-bottom-nav`) ganhou icones/texto maiores (icone `20px→40px`, texto `10px→13px`) e gap icone-texto reduzido ao minimo (`gap:1px`), mas a ALTURA da barra em si foi reduzida ao maximo depois (padding do botao `10px→4px`, padding do container `10px→6px`, `min-height` do botao `84px→64px`) — pedido explicito do usuario pra "baixar a altura ao maximo possivel" mantendo icone/texto grandes. Referencia visual original passada pelo usuario (`notao-grande.jpeg`/`botao-pequeno.jpeg` na raiz, nao versionados). Ajustado junto a cada rodada: `main` padding-bottom (atual: `94px`) e a posicao do botao flutuante `+` (atual: `bottom:79px`) pra acompanhar a altura da barra.
- Publicado em producao: usuario pediu pra tambem usar o wizard ao EDITAR um registro existente (antes so "criar novo" usava wizard, editar sempre abria o formulario completo — regra usada em todo o rollout ate aqui). Combinado: ativar primeiro so no mobile/tablet, testar por um periodo, depois migrar pro desktop tambem (por enquanto o `matchMedia(max-width:1024px)` continua igual, nenhum dialog virou wizard incondicional no desktop). Mudanca aplicada em TODOS os dialogs do admin que tinham essa distincao novo/editar (removido o `!item &&`/`!method &&` da chamada `.activate()`): `catalogDialog`, `paymentDialog`, `paymentMethodDialog`, `supplierDialog`, `supplierServiceDialog`, `supplierEntryDialog`, e o bespoke `#serviceDialog` (`setServiceWizardMode`). Tambem no `entryDialog` do portal do fornecedor (`fornecedor.js`), que ja era sempre-wizard pra novo e agora tambem é pra editar.
  - **Risco encontrado e corrigido no `#serviceDialog`** (o mais complexo, com Complementares/Fornecedor de multipla selecao): 3 bugs latentes que só importavam ao habilitar edicao no wizard, corrigidos em `app.js`:
    1. Editar um lancamento SECUNDARIO (complementar) tem os campos `hasAdditionalServices`/`hasSupplierService` desabilitados (`disabled`) porque um complementar nao pode ter os proprios complementares/fornecedor — mas os botoes Sim/Nao do wizard nao respeitavam `disabled` e deixavam trocar assim mesmo. Corrigido pulando as etapas 6/7/8 quando o campo esta `disabled` (`shouldSkipServiceWizardStep`).
    2. Tocar num card ja adicionado no picker de Complementares/Servico do fornecedor SEMPRE removia, mesmo se o item ja estivesse `locked` (ja em uma cobranca/conta a pagar — a lista de baixo ja respeitava isso escondendo o botao ×, mas o picker de cards nao). Corrigido: tocar num item locked agora mostra aviso em vez de remover.
    3. **O mais serio**: a etapa "Placa ou referência" sempre chamava `addCurrentReference()` ao validar, que ADICIONA a lista de multi-criacao E LIMPA o campo de texto — mecanismo pensado só pra lançamento novo (varias referencias = varios registros). Pro caso de EDITAR, o submit le o campo de texto diretamente (`existingEntry ? [typedReferences.join(" ")] : ...`), entao limpar o campo faria salvar a placa/referência EM BRANCO, apagando o dado. Corrigido: ao editar, a etapa so valida que o campo nao esta vazio, sem tocar na lista/limpar o campo. Tambem escondido o botao "+"/lista de referencias (`.edit-hide-native`) quando estiver editando (em qualquer tamanho de tela, nao so wizard), ja que esse "+" nunca fazia sentido pra editar um unico registro e tinha esse mesmo risco de limpar o campo mesmo fora do wizard.
- Publicado em producao: escondidos no mobile/tablet os 3 botoes de atalho no topo da tela Resumo (`.dashboard-heading .dashboard-actions`: Lançamento/Fornecedores/Financeiro) — a pedido do usuario, ja que ficaram redundantes com a barra de navegacao inferior e o botao flutuante `+`. So `display:none` dentro do `@media(max-width:1024px)` de `styles.css`; desktop continua igual.
- Publicado em producao e testado pelo usuario: no mobile/tablet, cabecalho do Resumo ganhou destaque visual (borda esquerda colorida, sombra, titulo maior) e os cards de servicos/financeiro passaram a aparecer ANTES dos filtros de periodo (ordem mobile: abas > cards > filtros > graficos/ranking); desktop mantem a ordem original (abas > filtros > cards > graficos), sem nenhuma mudanca visual. A secao `#dashboardServicesTab`/`#dashboardFinanceTab` de `index.html` foi dividida em duas cada (`#dashboardServicesCards`/`#dashboardServicesCharts` e `#dashboardFinanceCards`/`#dashboardFinanceCharts`) pra intercalar o bloco de filtros compartilhado (`.dashboard-period-controls`) entre cards e graficos — a reordenacao em si e so CSS (`#dashboard.active{display:flex;flex-direction:column}` + `order` em cada bloco, valores diferentes dentro do `@media(max-width:1024px)`), sem duplicar HTML nem mudar logica de dados. `renderDashboardV2` em `app.js` atualizado pra alternar visibilidade das 4 secoes novas (antes eram so 2).
- Publicado em producao e testado pelo usuario: todos os `alert()`/`confirm()` nativos do navegador foram substituidos por componentes proprios (`showAppAlert`/`showAppConfirm`) em `app.js`, `supplier.js` (admin) e `fornecedor.js` (portal do fornecedor, componente proprio e separado, mesma API mas nao exposto em `window`). Alertas informativos (sucesso/erro/aviso/info) aparecem como um toast leve que sobe no rodape da tela sem escurecer o fundo, fecha sozinho apos alguns segundos (duracao proporcional ao tamanho da mensagem) ou ao clicar nele; confirmacoes (Sim/Nao) continuam como caixa central com fundo escurecido, por exigirem decisao do usuario antes de prosseguir. Foram preenchidas tambem varias lacunas onde nenhuma confirmacao existia antes, nem nativa nem propria (criar/editar/cancelar/excluir lancamento, cliente, tabela de preco, servico do catalogo, pagamento, forma de pagamento, cobranca, pedido online, solicitante, fornecedor e seus lancamentos/contas/pagamentos). Configuracoes ganhou dois liga/desliga (`systemSettings.askEntryContinuation`/`systemSettings.offerSupplierShare`, cada um por aparelho): perguntar se quer lancar de novo (mesmo cliente/outro) apos salvar um lancamento novo, e oferecer o envio da solicitacao ao fornecedor pelo WhatsApp na hora — cada um em seu proprio card, ligado por padrao.
- Publicado em producao e testado pelo usuario: cliente ganhou campo `billingFrequency` (semanal/quinzenal/mensal, semanal por padrao — coluna `billing_frequency` em `supabase/client_billing_frequency.sql`, ja executado pelo usuario no Supabase). Nova aba "Atrasadas (N)" na tela Cobrancas (`#billing`), ao lado dos atalhos "Semana atual"/"Mes atual", com o numero de cobrancas atrasadas no proprio titulo da aba e destaque vermelho quando ha atrasos (`#billingOverdueTab`/`.has-overdue`). Uma cobranca e considerada atrasada quando a data de hoje passa do fim do periodo dela (`endDate`) e ela continua em aberto — sem nenhum dia extra de espera (`daysPastBillingPeriod`/`isBillingOverdue` em `app.js`). Cuidado: a primeira versao contava os dias desde a CRIACAO da cobranca com um limite variavel por frequencia (7/15/30 dias), o que fazia cobrancas de periodos ja fechados nao aparecerem como atrasadas ainda — corrigido para comparar com o fim do periodo (`endDate`), que e o que "vira a semana/quinzena/mes" realmente significa. O sino de alertas no topo do sistema ja usava a mesma logica internamente (`dashboardNotifications`/`activeAlertItems`), entao reflete os atrasos automaticamente, sem mudanca separada. O filtro de data da aba "Atrasadas" usa campos PROPRIOS e exclusivos (`billingOverdueStartFilter`/`billingOverdueEndFilter`, escondidos por padrao), separados dos campos da tela normal de Cobrancas (`billingStartFilter`/`billingEndFilter`) — a troca de visibilidade entre os dois pares acontece em `renderBillings()` conforme `billingOverdueOnly`. Historico do bug (ja corrigido): primeiro o filtro de data ficava totalmente ignorado no modo Atrasadas (nao dava pra combinar); depois, ao reaproveitar os MESMOS campos `billingStartFilter`/`billingEndFilter` da tela normal pra tentar combinar os dois, o `render()` (chamado a cada `saveState()`/sync remoto) sempre repunha a data da semana atual nesses campos via `syncFinancePeriodControls()`, sem saber que o modo Atrasadas estava ativo — o campo aparecia vazio por um instante e "voltava sozinho" pra data da semana. Corrigido usando campos de input separados que `syncFinancePeriodControls()` nunca toca.
- Publicado em producao e testado pelo usuario: nos cards de Clientes > Cadastro, o saldo aparece em azul e negrito quando o cliente tem valor em aberto (`.client-balance-due`), e o card fica com destaque vermelho quando o cliente tem alguma cobranca atrasada (`.client-overdue`, mesmo padrao visual do `.service-overdue`/`.receivable-overdue` ja existentes). Cabecalho do painel administrativo (`.topbar`) no desktop teve o padding vertical reduzido pela metade (24px para 12px) pra aproveitar melhor a tela; mobile/tablet nao foi alterado (ja tinha seu proprio padding menor, definido a parte no `@media`).
- Publicado em producao e testado pelo usuario: nova experiencia de menu mobile/tablet. Botao hamburguer (`#mobileMenuButton`) saiu do grupo de acoes a direita e foi para dentro do `.brand-lockup`, antes da logo; botao Sair (`#logoutButton`) deixou de ficar escondido no dropdown do hamburguer (removido `#mobileLogoutButton`) e passou a aparecer direto no topbar mobile, com CSS proprio (`.topbar-actions > #logoutButton`). As abas de Clientes/Fornecedores/Financeiro (`.client-area-tabs`/`.finance-area-tabs`/`.supplier-tabs`) ganharam icones (glifos Unicode monocromaticos — ex. `✎` lancamento, `▤` cadastros, `⚒` servicos, `▦` painel, `⇄` acessos, `▥` formas de pagamento, `▧` cobrancas — classe `.tab-icon`) tanto no mobile (empilhado, icone em cima) quanto no desktop (inline antes do texto). Fornecedores passou a abrir com as abas antes do titulo (`.section-heading` movido pra depois de `.supplier-tabs`), igual as demais paginas. Titulos de pagina (`.section-heading > div:first-child`) ficam em uma linha so — titulo grande + legenda antiga (eyebrow) discreta ao lado, via `order`/flex, sem mudar o HTML de cada pagina. Card "Sobre o aplicativo" (logo + AS TECH SOLUTIONS LTDA. + link GitHub) entrou em Configuracoes (`.settings-grid`), no lugar de um rodape no fim da pagina que o usuario nao gostou visualmente.
- Publicado em producao: barra de navegacao inferior mobile (`.mobile-bottom-nav`) teve a cor ajustada em rodadas, a pedido do usuario: fundo `color-mix(in srgb, var(--brand-soft) 50%, var(--surface) 50%)` (50% mais claro que a cor do sistema); icones/texto nao selecionados usam `var(--brand-ink)` (cor do sistema) no lugar do cinza `--muted`; item selecionado (`.mobile-bottom-nav-button.active`) com fundo solido `var(--brand)` e texto/icone branco.
- Corrigido bug (identificado por print enviado pelo usuario): titulo "Gestor de Servicos" no topbar mobile invadia os icones de alerta sonoro/notificacoes quando o texto era longo — faltava `min-width:0` no `<div>` que envolve eyebrow+h1 dentro de `.brand-lockup` (flex item sem isso nao encolhe, ignorando o `text-overflow:ellipsis` do h1). Corrigido com `.brand-lockup > div { min-width:0 }` no bloco mobile de `styles.css`.
- Publicado em producao: todos os pontos que abrem o WhatsApp pra enviar mensagem (`shareSupplierRequests` em `supplier.js`; `shareBillingByWhatsApp` e o submit do `trackingForm` em `app.js`) passaram a tentar abrir o **aplicativo** (`whatsapp://send?phone=...&text=...`) em vez de ir direto pro navegador. Helper `openWhatsApp(url, fallbackUrl)` (uma copia em `app.js`, outra no closure de `supplier.js`): dispara o esquema customizado via `window.location.href` (nao navega a pagina, so aciona o handler do SO) e fica de olho em `blur`/`visibilitychange` por 1,5s — se nada tomar o foco da tela (app nao instalado), cai automaticamente no fallback `api.whatsapp.com`/`web.whatsapp.com`, abrindo em nova aba (`window.open`) pra nao perder a aba do painel. Cuidado descoberto: `await navigator.clipboard.writeText(...)` ANTES de abrir o link (caso do grupo, em `shareSupplierRequests`) quebra a cadeia de gesto do usuario e o navegador bloqueia a navegacao silenciosamente — corrigido chamando o clipboard sem `await` (fire-and-forget) logo antes do `openWhatsApp`, mantendo tudo sincrono a partir do clique.
- Corrigido bug relatado pelo usuario (clicou varias vezes e gerou varias cobrancas pro mesmo cliente): o botao visivel do wizard mobile ("Fechar periodo"/"Gerar para todos") nao ficava desabilitado durante o `await` da criacao da cobranca (so o botao nativo escondido do form ficava) — cliques repetidos disparavam `form.requestSubmit()` de novo, cada um criando uma cobranca nova. Corrigido nos handlers `submit` de `billingForm` e `billingBatchForm` (`app.js`) com uma trava por formulario (`form.dataset.submitting`), liberada em todo `return` antecipado e no `finally`. Adicionado tambem botao "Excluir X cobranca(s) deste cliente" (`#billingDeleteClientButton`) na tela Cobrancas, visivel ao filtrar por um cliente especifico — apaga de uma vez todas as cobrancas daquele cliente (cancela acesso, libera lancamentos/pagamentos vinculados), pensado pra limpar duplicatas geradas pelo bug acima.
- Publicado em producao: ao cancelar um servico do cliente (`cancelServiceForm`) mantendo o servico do fornecedor vinculado (checkbox "Cancelar e zerar tambem o servico do fornecedor" desmarcada), o servico do fornecedor agora avanca automaticamente pro status `Entregue` (antes ficava parado no status anterior). Card do dashboard "Servicos complementares" virou "Servicos Fornecedores" — total de lancamentos de fornecedor (qualquer origem) no periodo, com `data-open-view="suppliers"` pra abrir Fornecedores ao clicar; nova funcao `supplierEntriesFor(range)`/campos `supplierEntries`/`supplierTotal` em `serviceMetrics()` (`app.js`), classe CSS `.metric-supplier` (renomeada de `.metric-complementary`).
- Publicado em producao: "Pedido on-line" ganhou 2a aba, "Links gerados" (`#trackingLinksTabPanel`, alternada via `data-request-tab`/`activeRequestsTab` em `app.js`, mesmo padrao do `.dashboard-inner-tabs`), listando um card por cliente com link/identificador/senha guardados em texto pra copiar de novo a qualquer momento (antes so hash, irrecuperavel). Novas colunas em `service_tracking_links` — `plain_access_code`, `plain_full_token`, `plain_identifier`, `plain_password` — em `supabase/tracking_links_recoverable_credentials.sql` (precisa do `service_tracking_links.sql` ja aplicado antes; atencao, o usuario tem mais de um projeto Supabase — o correto de producao e `tfkhhxopxupbefaaijcz`, ja aconteceu de rodar SQL no projeto errado por engano). Novo endpoint `netlify/functions/admin-tracking-links.mjs`: GET lista os links `active=true` (join manual com `clients` pra nome) com as credenciais em texto; DELETE (`{id}`) cancela um link na hora (marca `active=false` e zera os campos `plain_*`, tornando a senha realmente irrecuperavel, nao so "inativa"). `issue-service-tracking-link.mjs` mudou pra sempre gerar `fullAccessCode` + `identifier`/`password` juntos (removido o parametro `passwordMode`, a escolha entre os dois modos) e invalidar TODOS os links ativos anteriores do cliente (antes so invalidava do mesmo periodo exato — por isso apareceram clientes com ate 4 links ativos simultaneos, sobra de testes antes dessa correcao). O wizard do `trackingDialog` caiu de 8 pra 6 passos (removidos "Acesso completo (com senha)" e "Abas do acesso completo"/Financeiro-Relatorio); passo de confirmacao sempre mostra o resultado (link + identificador + senha, 3 botoes de copiar) e ganhou botao "Fechar" explicito (o X do dialog-heading ficava pouco visivel dentro do wizard mobile). Cards de "Links gerados" e o painel de resultado do dialog usam fonte monoespacada pros codigos e rotulos em caixa alta (`.tracking-link-field`/`.tracking-access-credentials`). Botoes "Gerar novo link" e "Atualizar" dentro da propria aba "Links gerados". Bug corrigido: a aba nao atualizava se fosse aberta antes de existir link (variavel `trackingLinksLoaded` guardava "ja carregou" e nunca buscava de novo) — removida essa trava, sempre busca de novo ao entrar na aba. Cada card tem botao "Excluir" (cancela o acesso na hora, mesmo efeito do DELETE acima).
- Publicado em producao: portal de acompanhamento (`acompanhamento.js`/`.html`/`.css`) passou a mostrar servicos cancelados (backend so parou de filtrar `status=neq.Cancelado` na consulta do periodo em `service-tracking-data.mjs`; mantido o filtro nas consultas de Financeiro/faturamento) — ficam numa secao propria no fim da aba Servicos (`#cancelledServiceList`), com card "Cancelados" no resumo do topo (`#trackingSummary`); `statusData()` ganhou o caso `Cancelado`. Filtros de servico ganharam datas inicial/final (`#trackingServiceStartFilter`/`#trackingServiceEndFilter`) e viraram uma linha so via flexbox (`.tracking-search`). Relatorio de cobranca ganhou observacao fixa informando o periodo coberto e que nao inclui servicos em aberto (`.tracking-billing-note`). Aba Financeiro ganhou um 4o card, "Total em aberto + nao faturado" (`.summary-grand-total`, cor roxa pra nao confundir com o card vermelho de cancelados), somando saldo em aberto da cobranca com o consumo ainda nao faturado — os 3 cards antigos (Consumo nao faturado/Cobranca atual/Saldo em aberto) foram mantidos como estavam, a pedido do usuario.
- Ajustado com o usuario, depois de mais de uma rodada: a tela de entrada do link de acompanhamento ("Entrar sem senha"/"Entrar com senha") estava sendo pulada sempre que o link vinha com o token de acesso completo embutido (`&full=`), porque `loadTracking()` ja aplicava as credenciais guardadas antes de checar se precisava mostrar a escolha. Corrigido: na primeira visita (sem escolha salva na sessao), a busca inicial acontece SEM credenciais, forcando resolver como restrito e sempre mostrar a tela de escolha pra link "gated"; so aplica a senha guardada em visitas seguintes na mesma sessao (`storedChoice`). Modelo final: "sem senha" = restrito, sem Financeiro; "com senha" = acesso irrestrito, incluindo Financeiro (`full_show_financial`/`full_show_billing` = `true` em `issue-service-tracking-link.mjs`, depois de uma rodada intermediaria em que tinham ficado `false`). "Entrar com senha" agora sempre exige digitar identificador e senha no formulario (`enterFullAccess` parou de espalhar `storedTrackingCredentials()`/o token embutido — so usa o que foi digitado), a pedido explicito do usuario; o token `full=` da URL fica guardado na sessao mas nao e mais usado pra pular a digitacao.
- Corrigido bug relatado pelo usuario: no modal "Novo pedido" do acompanhamento, o Enter dentro dos `textarea` de Placa/referencia e Observacao avancava a etapa do wizard em vez de quebrar linha (o listener de Enter so excluia `tagName === "BUTTON"`, nao `"TEXTAREA"`). Corrigido em `acompanhamento.js`.
- Corrigido bug relatado pelo usuario (vazamento de valores): apos escolher "Entrar sem senha" no link de acompanhamento gated, qualquer recarga (ex.: `refreshTracking()` disparado ao enviar um pedido) voltava a mandar as credenciais completas guardadas na sessao (incluindo o token `full=` da URL, salvo independente da escolha), reclassificando o acesso como completo no backend e mostrando valores que deveriam ficar ocultos. Corrigido em `loadTracking()` (`acompanhamento.js`): so reenvia `storedTrackingCredentials()` quando `storedChoice === "full"`; escolha "restricted" nunca reenvia credenciais.
- Corrigido bug relatado pelo usuario: os cards de resumo do acompanhamento (A fazer/Feitos/Entregues/Total/Cancelados) e o contador da lista contavam servicos complementares junto com o principal, dobrando a quantidade exibida quando havia complementares. Corrigido contando so `!item.is_secondary` pra quantidade; valores em dinheiro continuam somando tambem os complementares.
- Publicado em producao: modal "Novo pedido" do acompanhamento ganhou botoes de escolha (igual ao padrao do lancamento admin) — etapa "Qual servico?" mostra os servicos do cliente como botoes paginados ordenados pelos mais usados (`trackingServiceUsageItems`/`renderTrackingServicePicker` em `acompanhamento.js`, mesmo padrao de `catalogUsagePickerItems` do admin); etapa "Quem solicitou?" ganhou botoes de sugestao com solicitantes ja cadastrados (mais usados primeiro) + opcao "Nao informar", mantendo campo de digitar/adicionar escondido atras de um botao "Buscar"/"Buscar/novo" (`revealTrackingPickerSearch`). CSS novo: `.tracking-picker*`/`.tracking-choice-btn` em `acompanhamento.css`.
- Corrigido bug relatado pelo usuario (`04 - CRV s/Cadastro` no wizard mobile do lancamento: botao existia mas nao selecionava): o clique no botao do catalogo reconstruia o texto do rotulo e reencontrava o item por correspondencia EXATA de texto (`itemByExactLabel`) em vez de usar o `id` do proprio botao — se outro item de catalogo tivesse o mesmo rotulo calculado (ex.: codigo digitado dentro do campo nome por engano, colidindo com outro item com codigo real), a selecao ficava ambigua, e a revalidacao do "Continuar" refazia a mesma busca por texto e sobrescrevia a selecao correta. Corrigido em `app.js`: clique no picker de catalogo (`resolveServiceCatalogSearchOnEnter`-like, dentro do handler de clique do `serviceDialog`) seleciona direto pelo `id` do botao; `syncServiceCatalogSelection()` so refaz a busca por texto quando o campo de busca foi realmente re-digitado (preserva selecao ja feita por botao). Tambem: `searchableText()` passou a colapsar espacos duplicados/nas pontas (nao so remover acento/caixa), o que resolveu um caso persistente de um item especifico so funcionando apos apagar e redigitar manualmente.
- Publicado em producao (pedido explicito do usuario: "padronizar todos os modais"): `createDialogWizard()` (compartilhado por `app.js` e `supplier.js`) ganhou resolucao generica de campo de busca no Enter — qualquer campo registrado em `pickers` (codigo/nome) reescreve o rotulo completo ao apertar Enter, tanto no desktop quanto no wizard mobile, preservando o avanco de foco/etapa que cada dialogo ja tinha (isso foi ajustado numa rodada: a primeira versao parava de avancar depois de resolver, o usuario pediu de volta o avanco automatico "como antes"). Isso cobriu de uma vez `billingDialog`, `billingBatchDialog`, `trackingDialog`, `catalogDialog`, `paymentDialog`, `paymentMethodDialog` (`app.js`) e `supplierDialog`, `supplierServiceDialog`, `supplierEntryDialog`, `supplierPayableDialog`, `supplierPaymentDialog`, `supplierAccessDialog` (`supplier.js`). De brinde, corrigiu nesses mesmos modais o Enter dentro de um `textarea` (ex.: motivo de cancelamento) que antes avancava a etapa em vez de quebrar linha. Campo Cliente em `paymentForm`/`billingForm`/`trackingForm` tambem passou a reescrever o nome completo ao confirmar com Enter (registrado como entrada em `pickers` mesmo sem grade visual de botoes, so pra reaproveitar o mecanismo generico). No `#serviceForm` (bespoke, fora do `createDialogWizard`): campo Cliente passou a aceitar digitar so parte do nome (`uniqueClientMatch`, ja existente em outras telas) e mostra aviso especifico quando a busca parcial e ambigua (mais de um cliente encontrado) em vez de negar silenciosamente. No modal "Novo pedido" do acompanhamento: campo Placa/referencia deixou de ser um `textarea` de multiplas linhas e virou uma lista tipo chip (digitar e Enter adiciona a lista e limpa o campo pra proxima placa; Enter com campo vazio avanca a etapa — mesmo padrao ja usado no campo "reference" do admin/`addCurrentReference`); campo Observacao, se vazio e der Enter, so move o foco pro botao Continuar (nao avanca sozinho); com texto, Enter continua quebrando linha normalmente.
- Publicado em producao: catalogo de servicos ganhou codigo em auto-incremento — ao abrir "Novo servico" (`openCatalogForm(null)`), o campo Codigo numerico ja vem preenchido com o maior codigo numerico atual + 1 (`nextCatalogCode()` em `app.js`), texto selecionado pra sobrescrever rapido se quiser outro numero; editar item existente nao muda.
- **Projeto de notificacoes push implementado e publicado** (planejado com o usuario antes de codar, ver arquitetura completa nos arquivos abaixo). Objetivo: avisar admin de pedido novo/atraso mesmo com o app fechado, ja que o alerta antigo (`notifyNewClientRequests`/toast/som) so funciona com a pagina aberta (`setInterval(refreshRemoteState, 8000)`, throttled pelo navegador em segundo plano).
  - Banco: `supabase/push_subscriptions.sql` (ja rodado pelo usuario no projeto `tfkhhxopxupbefaaijcz`) — tabelas `push_subscriptions` e `push_notified_alerts`.
  - Primeira dependencia npm do projeto: `web-push` (`package.json`/`package-lock.json` na raiz). Chaves VAPID geradas localmente com `npx web-push generate-vapid-keys` e cadastradas nas variaveis de ambiente do Netlify pelo proprio agente via `netlify env:set` (autorizado explicitamente pelo usuario com "Quero que voce resolva por ai" apos o usuario inicialmente preferir cadastrar manualmente).
  - Funcoes novas: `push-subscribe.mjs`, `push-unsubscribe.mjs`, `push-overdue-check.mjs` (agendada, `*/30 * * * *`), helper `_shared/push.mjs` (`sendPushToAllAdmins`, com `urgency:"high"`/`TTL:3600` pra reduzir atraso de entrega quando o navegador esta fechado).
  - `sw.js`: listeners `push` (mostra notificacao) e `notificationclick` (foca a aba existente e chama `client.navigate(url)` pra ela pular pra view certa, ou abre nova janela). `app.js` le `location.hash` na inicializacao (`applyNotificationDeepLink`, chamada logo apos `render()` em `initializeRemoteState`) e troca de view — pedido novo manda `url:"/#requests"` (view `requests` = aba Pedido on-line).
  - Configuracoes ganhou card "Notificacoes push" com botao ativar/desativar por aparelho (`settingsPushToggle`/`updatePushToggleButton`/`enablePushNotifications`/`disablePushNotifications` em `app.js`), pedindo permissao do navegador e registrando a inscricao via `push-subscribe`.
  - **Bug corrigido (achado via `netlify logs --source functions --function <nome> --since 48h`, so descoberto porque o log da funcao "certa" mostrava zero chamadas apos os testes do usuario)**: o disparo de push de pedido novo tinha sido colocado so em `client-service-request.mjs`, mas o modal "Novo pedido" do acompanhamento (o unico testado) chama `tracking-service-request.mjs` — uma funcao diferente que grava na mesma tabela mas por outro portal/autenticacao. Corrigido adicionando `sendPushToAllAdmins()` tambem em `tracking-service-request.mjs`.
  - **Investigacao concluida — push no PC (Windows) exige Edge, nao funciona com Chrome fechado**: Android testado e funcionando perfeitamente (navegador fechado, notificacao chega normal). No PC (Windows 10), testado e diagnosticado a fundo com o usuario: no Chrome, mesmo com todas as configuracoes corretas (`background_mode.enabled=true` no `Local State`, permissao de notificacao do site em "Permitir", canal de notificacao do Chrome liberado no registro do Windows, Foco Assistente desligado), o processo `chrome.exe` morre por completo ao fechar todas as janelas (confirmado: sem icone na bandeja, sem `chrome.exe` no Gerenciador de Tarefas) — o toggle "continuar rodando em segundo plano" do Chrome so tem efeito pra extensoes/apps do modelo antigo "Chrome Apps", nao pra PWAs comuns, entao nao segura o processo. Sem processo vivo, nao ha como o push chegar. **Nao existe configuracao que resolva isso no Chrome puro** — e limitacao inerente do Chrome no Windows (falta integracao nativa com o WNS do Windows pra push). Solucao adotada: instalar o painel como app (PWA) no **Microsoft Edge** (`⋯ > Apps > Instalar este site como um aplicativo`) em vez de/alem do Chrome — o Edge tem integracao nativa com o WNS do Windows e entrega push normalmente mesmo com o navegador totalmente fechado. Testado e confirmado pelo usuario: notificacao apareceu na Central de Notificacoes do Windows com o Edge fechado. Recomendacao pro usuario: usar o app instalado no Edge no PC pra depender de push quando o painel nao estiver aberto; no Chrome, push so chega com o navegador aberto/minimizado. iPhone precisa do app "Adicionado a Tela de Inicio" (instalado como PWA) e iOS 16.4+ pra push funcionar; nao funciona so no Safari normal.
- Corrigido bug relatado pelo usuario: no modal "Novo pedido" do acompanhamento, o teclado abria e a tela dava zoom automatico ao focar o campo Placa/referencia, e o zoom nao voltava ao normal mesmo com o teclado recolhido. Causa: os inputs de `acompanhamento.css`/`fornecedor.css` usavam `font:inherit`, herdando `font-size:12px` do `<label>` pai (que estiliza so o rotulo) — abaixo do limite de 16px que navegadores mobile usam pra decidir se dao zoom automatico ao focar um campo. Corrigido com `font-size:16px` explicito em `input, select, textarea` nos dois arquivos.
- Publicado em producao: data/horario de cada fase do servico. No acompanhamento (cliente), mostra "Entregue em DD/MM/AAAA HH:mm" discretamente no card do servico, so quando o status e Entregue (`service-tracking-data.mjs` passou a expor `delivered_at` nas consultas de servicos). No admin, `serviceStatusDates()` (`app.js`) e o equivalente inline em `supplier.js` (lista de fornecedor) passaram a mostrar "Lancado em / Feito em / Entregue em" pra cada fase ja ocorrida, em qualquer status (antes o admin so mostrava Feito/Entregue, e a lista do fornecedor nao mostrava nenhuma data). Botao "Voltar para A fazer" adicionado quando o status e Feito — a logica de `applyServiceStatus` ja suportava voltar, faltava so o botao na tela.
- **Bug serio corrigido (perda de dado em producao)**: editar um lancamento salvava sem a referencia mesmo digitando ela. Causa: no campo Placa/referencia do `#serviceDialog` (wizard mobile em `app.js` perto de `document.getElementById("serviceForm").addEventListener("keydown"` e formulario desktop classico mais abaixo no mesmo arquivo), pressionar Enter chamava `addCurrentReference()` incondicionalmente — funcao pensada so pra lancamento NOVO (adiciona a lista de multi-criacao e limpa o campo pra proxima placa). Como o submit ao EDITAR le o campo diretamente (nao a lista), o Enter durante a edicao limpava o valor antes de salvar. Corrigido checando `form.elements.entryId.value` (edicao) nos dois pontos — nesse caso o Enter so confirma/avanca, sem limpar. Isso tambem explicava a mesma falta de referencia no servico do fornecedor gerado a partir do lancamento do cliente (`createForClientEntries` copia `entry.reference` no momento da criacao — se ja estava vazio pelo bug acima, o fornecedor herdava o vazio). Corrigido tambem, separadamente: ao editar um lancamento com servico complementar, a referencia/data do complementar nunca era sincronizada com a do principal e ficava desatualizada — corrigido em `app.js` (loop que atualiza os complementares mantidos ao salvar a edicao).
- **Regra adotada pelo usuario pra todo campo do sistema, aplicada em varredura completa (admin, fornecedor, portal cliente, acompanhamento)**: (1) todo campo que digita nome/codigo de um registro existente precisa ter autocomplete (datalist ou picker de botoes) — a auditoria nao achou nenhuma violacao, ja era seguido em todo lugar; (2) em TODO campo, Enter deve avancar pro proximo campo (ou confirmar/submeter no ultimo), exceto textarea genuino de texto livre (observacoes, motivo de cancelamento), onde Enter deve continuar quebrando linha normalmente. Achados da regra (2) corrigidos: handler global de Enter em `app.js` fazia `preventDefault` num textarea mas nao parava ali (sem `return`), caindo na logica de avancar/submeter — afetava `cancelServiceDialog` (podia cancelar o servico com o motivo incompleto no meio da digitacao), `clientDialog` (Observacoes salvava o cliente prematuramente) e `paymentMethodDialog` no desktop; `supplierEntryDialog` tinha dois listeners de Enter conflitando no mesmo formulario, Observacoes nunca quebrava linha e submetia o lancamento do fornecedor pulando a etapa de confirmacao; no pedido do cliente (`cliente.js`), Enter em Placa/referencia avancava em vez de quebrar linha (impedia digitar varias placas) e Enter em Observacao submetia o pedido inteiro; login do portal do cliente e a tela de senha do acompanhamento nao tinham tratamento de Enter (tentava submeter direto do campo Identificador); formulario "Como deseja receber?" do fornecedor nao tinha nenhum tratamento de Enter. Alem disso, as BARRAS DE FILTRO (Lancamentos, Fornecedores no admin, busca do acompanhamento, filtros do portal do cliente) sao `<div>` simples, nao `<form>`, entao o Enter nao fazia nada nelas — adicionado um handler generico por portal (`app.js`/`supplier.js`: classe `.filters`; `cliente.js`: `.client-filters`; `acompanhamento.js`: `.tracking-search`) que avanca o foco pro proximo campo visivel da mesma barra; no ultimo campo so mantem o foco, ja que o filtro e ao vivo e nao tem "confirmar". Padrao a seguir daqui pra frente: qualquer handler novo de Enter/textarea nunca deve fazer `preventDefault` sem um `return` imediato logo depois — deixar o Enter quebrar linha nativamente em textarea, nunca cair na logica de avancar/submeter.
- **Pacote grande publicado em producao: reformulacao de Financeiro e reorganizacao de Fornecedores** (planejado com o usuario antes de codar, `pack.md` na raiz, nao versionado; correcoes de teste em `correcao.md`, tambem nao versionado). SQL novo ja executado pelo usuario no Supabase (`tfkhhxopxupbefaaijcz`): `supabase/billing_number.sql` e `supabase/supplier_portal_recoverable_credentials.sql`.
  - Financeiro: cobrancas ganharam numeracao sequencial global (`billingNumber`, `nextBillingNumber()` em `app.js`, mesmo padrao do `nextCatalogCode()`), exibida em cards/PDF/WhatsApp/busca. Pagamentos passaram a ter 3 estados de exibicao em vez do badge generico "Abatido": `paymentAllocationState()`/`paymentAllocationLabel()` (`app.js`) distinguem "parcial solto sem cobranca", "vinculado a cobranca em aberto" e "quitacao"; lista de pagamentos (`renderPayments`) agrupada por cliente, e a lista de cobrancas do periodo em Pagamentos tambem virou lista cronologica agrupada por cliente (sem cards). Nova aba "Resumo por cliente" em Financeiro (`renderFinanceSummary`, `#financeSummary`) mostra por cliente: cobranca anterior, consumo do periodo, pago no periodo e saldo em aberto acumulado — cobre o cenario de servico->cobranca->pagamento->novo servico sem fechar cobranca. **Bug corrigido apos teste do usuario**: a formula inicial usava so `balanceFor()` (saldo cumulativo simples), que mascarava o saldo quando um pagamento do periodo quitava uma divida ANTERIOR (saldo aparecia igual ao consumo, escondendo o pagamento) — corrigido com `previousBalanceFor()`, que soma de volta ao "saldo anterior" o que foi pago dentro do periodo (evita contar a baixa duas vezes); testado em `tests/finance-summary-balance.test.mjs` com os 3 cenarios exatos relatados pelo usuario.
  - Fornecedores: submenu reordenado (Lancamentos, Acessos, Cadastros, Contas a pagar, Painel — Lancamentos e a aba padrao ao abrir Fornecedores, nao mais Painel). Cadastros: cada card de fornecedor ganhou botao "Servicos" que abre modal (`supplierServicesDialog`) com a tabela de servicos so daquele fornecedor, selecao por checkbox no topo (sem editar/excluir por linha), linhas zebradas compactas. Contas a pagar: filtro de data/busca, periodo padrao semana atual com atalhos semana/mes (reaproveita `financePeriod` global via `syncFinancePeriodControls`), historico de baixas deixou de ser lista global e virou botao "Historico de pagamento" expansivel por conta. Acessos ganhou 2 abas: "Servico online" (lista `state.supplierEntries` com `source === "Fornecedor"`, so UI) e "Links gerados" — portou pro fornecedor o mesmo modelo com/sem senha que o cliente ja tinha: sem senha = restrito (sem financeiro, servicos sem valor), com senha = acesso completo, reaproveitando `resolveTrackingTier()` (`_shared/server.mjs`, generico o bastante pra servir os dois). Funcoes novas/reescritas: `issue-supplier-link.mjs`, `supplier-portal-data.mjs`, `netlify/functions/admin-supplier-links.mjs` (novo); portal do fornecedor (`fornecedor.html`/`.js`/`.css`) ganhou a mesma tela de escolha "Entrar sem senha/Entrar com senha" do acompanhamento do cliente. Permissoes granulares (lancar/marcar feito/cancelar) continuam independentes do nivel de acesso, por decisao explicita do usuario.
  - **Painel de fornecedores oscilou de lugar durante os testes do usuario**: primeira tentativa foi mover pro Resumo geral como 3a aba exclusiva junto de Servicos/Financeiro (`dashboard-inner-tabs`) — usuario reportou que os dados nao apareciam; segunda tentativa foi virar um painel independente que abria abaixo do Resumo sem esconder Servicos/Financeiro — usuario esclareceu que queria o mesmo mecanismo de aba exclusiva de Servicos/Financeiro, e os dados continuavam sumindo; decisao final foi desistir de colocar no Resumo e manter o Painel dentro da propria secao Fornecedores (aba "Painel", `#supplierDashboardPanel`), preservando as melhorias feitas no caminho: filtro por fornecedor com autocomplete (nao mais picker de cards por volume), atalhos "Semana atual"/"Mes atual" (`data-supplier-dashboard-period`), cards de servico numa linha so (`dashboard-status-cards`) e clicaveis.
- **Ainda nao publicado (local): reformulacao do submenu Pagamentos (Financeiro)** — primeiro submenu de uma revisao geral do menu Financeiro pedida pelo usuario, submenu por submenu (`pagamentos.md` na raiz, nao versionado, guia o replanejamento; regra do usuario: "cada submenu deve fazer uma unica coisa e bem feita"). Pagamentos virou um caixa registrador puro, sem duplicar a aba Cobrancas:
  - Removida a lista "Cobrancas do periodo" (Baixa parcial/Quitar) que duplicava a aba Cobrancas; Pagamentos agora so registra e lista pagamentos (`renderPayments` reescrita em `app.js`).
  - Cards trocaram de "saldo em aberto" pra "Recebido" (periodo anterior/hoje/periodo atual). `previousOperationalWeek` foi generalizada pra `previousFinancePeriod(period, mode)`, respeitando semana/mes (antes so calculava semana, mesmo no modo mes).
  - **Bug de logica corrigido**: registrar pagamento direto (sem passar por uma cobranca especifica) sempre nascia com `billingId` vazio, mesmo com cobranca aberta existente pro cliente — ficava "solto" ate alguem achar a cobranca certa. Novo `latestOpenBillingFor(clientId)` (a cobranca valida e sempre a ultima gerada com saldo) detecta e vincula automaticamente ao digitar o cliente (`updatePaymentBillingHint`, chamada em `syncPaymentClientSelection` so pra pagamento NOVO — nunca mexe na cobranca de um pagamento em edicao); sem cobranca aberta, avisa que vira credito.
  - Pagamento acima do saldo da cobranca vinculada: antes bloqueava com alerta ("valor maximo e X"); agora aplica o valor devido e gera automaticamente um segundo pagamento de credito com a diferenca, mesmo padrao do `allocateAdvancePayments` no fechamento de cobranca — consistente com a regra ja documentada ("se pagamento passar do valor devido, tratar como credito"); mudanca de comportamento confirmada explicitamente com o usuario antes de implementar.
  - Historico virou tabela (Data/Cliente/Valor) com selecao de linha por clique e botao "Detalhes" (icone "⋮", `.row-options-button`) no topo da lista, abrindo `paymentDetailDialog`: mostra alocacao (credito/parcial/vinculado/quitacao como pastilha colorida — `.payment-status-pill`, mesma paleta das cobrancas `.billing-aberta`/`.billing-parcial`/`.billing-paga`), cobranca/periodo/saldo quando houver, e consumo do periodo atual ainda nao faturado (`unbilledConsumptionFor`, soma servicos da semana operacional atual quando nenhuma cobranca ainda cobre o periodo). Editar so aparece pra pagamento sem `billingId` (regra ja existente preservada: pagamento vinculado a cobranca nao pode ser editado); Excluir sempre disponivel.
  - Botao "Registrar pagamento" saiu do topo da pagina e virou um icone "+" (`.button-accent`) ao lado do botao de Detalhes, no cabecalho da lista "Pagamentos registrados".
  - Atalhos do Resumo (`data-payment-dashboard-filter`: cards "Cobrancas em aberto"/"Em aberto"/legendas Pagas-Parciais-Aberto) que antes apontavam pro filtro de status removido de Pagamentos foram redirecionados pra aba Cobrancas (`billingStatusFilter`/`billingOverdueOnly`), com rotulos ajustados ("Ver cobrancas"/"Abrir cobrancas").
  - **Bug de CSS real corrigido** (achado via print do usuario, `modal-defeito.png`): `.payment-detail-summary-row span` (regra do rotulo a esquerda) tinha mais especificidade CSS que `.payment-status-pill`, apagando a cor da pastilha de situacao (aparecia cinza/sem cor). Corrigido dando ao rotulo uma classe propria (`.payment-detail-summary-row-label`) em vez de selecionar por `span` generico — verificado renderizando o HTML/CSS isolado num Chromium headless (Playwright, instalado a parte na pasta de scratch, sem virar dependencia do projeto) antes e depois da correcao.
  - Modal de detalhes passou por varias rodadas de ajuste fino de estilo a pedido do usuario, que foi mandando prints de referencia nao versionados (`modal.png`, `modal-defeito.png`, `modal-movel.jpeg`, `padrão-modal.png`): removido o layout de blocos coloridos (`callout`) em favor de uma lista de linhas uniformes (mesmo padrao visual dos resumos de wizard `.wizard-summary-row`/`.tracking-wizard-summary-row`, so que sempre visivel, nao so no wizard mobile); removida uma linha divisoria que tinha sido inventada sob "Pagamento de/Nome/Valor" (os outros modais do sistema nao tem essa borda interna); botoes do rodape (Fechar/Excluir/Editar) reformulados pra bater com o padrao dos demais modais — no desktop ficam compactos e alinhados a esquerda (Fechar so texto sem fundo, Excluir/Editar em pilula bem arredondada), tudo numa classe propria (`.payment-detail-actions`) sem tocar em `.dialog-actions`/`.primary`/`.secondary` compartilhados por todos os outros dialogs; no mobile continuam esticados/empilhados em largura total (`min-height:48px`), igual ao botao "Continuar" do wizard (`Editar` fica por ultimo, mais perto do polegar).
  - Servidor local: nesta sessao, o usuario testou por engano na porta 8000, que e outro projeto Django dele ("Agenda Integra") rodando na mesma maquina, nao o gestor_servicos — o certo e sempre `node server.js` na porta **8080**. Vale conferir a porta antes de assumir que uma mudanca "nao pegou".
  - **Depois, decisao revertida pelo usuario**: o `.payment-detail-actions`/`.payment-detail-actions-primary` bespoke (pilulas `border-radius:999px`, sem usar `.dialog-actions`/`.primary`/`.secondary`/`.danger-button`) foi abandonado a pedido explicito do usuario ("quero que ele fique com as mesmas configuracoes dos modais do sistema"). `#paymentDetailDialog` passou a usar exatamente o padrao dos demais dialogs: conteudo dentro de `.payment-detail-content` (`padding:24px`, mesmo principio de `.notification-content`/`.remote-load-content`) e rodape com `.dialog-actions` puro (Fechar/Excluir/Editar com as classes `.secondary`/`.danger-button`/`.primary` compartilhadas), so com um seletor extra `#paymentDetailDialog .dialog-actions{grid-template-columns:repeat(3, minmax(0,1fr))}` pra caber 3 botoes iguais (mesmo idioma de grid ja usado em `.tracking-access-actions`/`.supplier-access-actions`). Isso tambem corrigiu de graca um problema latente: por nao ser `<form>`, o dialog nunca teve o padding padrao (`dialog form{padding:24px}`) nem o comportamento mobile sticky de `.dialog-actions` — ficava colado nas bordas. Testado renderizando o dialog isolado (HTML/CSS reais do projeto) num Chromium headless antes/depois, e confirmado pelo usuario ao vivo no navegador (`node server.js`, Financeiro > Pagamentos > Detalhes).
  - **Tabela "Pagamentos registrados" com rolagem interna de 8 linhas**: com muitos pagamentos a pagina crescia e escondia a barra com os botoes "+" (Registrar pagamento) e "⋮" (Detalhes), que ficam acima da tabela. Corrigido so com CSS, escopado em `#paymentList` pra nao afetar outras tabelas (`.catalog-table-wrap`): `max-height:496px;overflow-y:auto` (496px = cabecalho ~47.5px + 8 linhas de ~56px, medido renderizando a tabela real num Chromium headless) e `thead th{position:sticky;top:0;background:var(--surface)}` pra "Data/Cliente/Valor" nao sumirem ao rolar. Como e `max-height` (nao `height` fixo), listas com menos de 8 pagamentos nao ganham espaco vazio sobrando. Usuario confirmou explicitamente que quer o mesmo padrao (8 linhas + rolagem interna + cabecalho fixo) estendido pra todas as listas longas do sistema (Cobrancas, Contas a pagar etc.) "em breve" — por enquanto ficou so em Pagamentos, nao iniciar a extensao sem o usuario pedir.
  - **Cards de "Recebido" (anterior/hoje/periodo) respeitam o filtro de cliente**: antes somavam sempre todos os clientes, mesmo com um cliente selecionado no filtro de Pagamentos. `sumPaymentsIn()` em `renderPayments()` (`app.js`) ganhou um filtro por `clientFilter`; quando ha cliente selecionado, os 3 cards mostram so os valores daquele cliente e a linha pequena de cada card ganha o nome do cliente (` · Nome`) pra deixar claro que esta filtrado. Sem cliente selecionado, os cards voltam a somar todos, como antes.
  - **Tabela de Pagamentos e modal de Detalhes melhorados pra mobile**: achados renderizando o app real com dados de estresse (nome de cliente longo, observacao longa, pagamento vinculado a cobranca) num Chromium headless antes de mexer no codigo.
    - Tabela: nome de cliente longo quebrava em varias linhas no mobile, deixando as linhas com alturas diferentes e invalidando o calculo das "8 linhas visiveis". Corrigido truncando o nome numa linha so com reticencias (`.payment-history-client{max-width:0;width:100%;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}`, mesma tecnica que `.metric-card strong` ja usa pra valores longos), escopado dentro do `@media(max-width:1024px)` — desktop continua mostrando o nome completo. Nome completo sempre disponivel ao abrir Detalhes.
    - A etiqueta pequena `· Nome do cliente` (adicionada nos cards de Recebido) ficava escondida no mobile por uma regra global do sistema (`.metric-card small{display:none}`, usada pra economizar espaco nos varios cards do Resumo geral). Reativada so pros cards de Pagamentos (`#paymentSummary .metric-card small{display:block}`), sem tocar na regra global.
    - **Bug real corrigido no modal de Detalhes** (nao so estetico): `#paymentDetailDialog` e o unico dialog do sistema que nao usa `<form>` por dentro (usa `.payment-detail-content`), entao nunca tinha ganho o tratamento de rolagem interna que todo dialog grande ja tem no mobile (`dialog[open]:not(.wide-dialog) form{flex:0 1 auto;min-height:0;overflow-y:auto;...}`). Com nome de cliente comprido + observacao longa + cobranca vinculada (mais linhas de resumo), o conteudo passava da tela e as ultimas linhas ("Saldo atual da cobranca", "Consumo nao faturado") ficavam escondidas atras do rodape fixo de botoes, sem como rolar ate elas — dado inacessivel, nao so feio. **Primeira correcao** (incluir `.payment-detail-content` no mesmo seletor `overflow-y:auto` que `form` ja usa, com `.dialog-actions`/`.dialog-heading` `position:sticky` dentro dele) funcionou no Chromium headless mas o usuario reportou em teste real no celular que a barra de botoes continuava sobrepondo o conteudo mesmo rolando — suspeita de comportamento inconsistente de `position:sticky` dentro de um container `overflow-y:auto` que fica em cima de um `<dialog>` (top layer) em alguns navegadores mobile. **Correcao definitiva**: parou de depender de `position:sticky` — reestruturado pra cabecalho e rodape de botoes ficarem FORA da area que rola, como irmaos flex diretos de `.payment-detail-content` (`display:flex;flex-direction:column`, `overflow:hidden`), com um novo wrapper `.payment-detail-scroll` (`flex:1 1 auto;min-height:0;overflow-y:auto`) envolvendo so `#paymentDetailBody`/nota de edicao. Assim a barra nunca pode ficar em cima do conteudo, por construcao geometrica, independente de navegador. Confirmado com teste programatico de scroll (Playwright, `showModal()` nativo) que nao ha mais sobreposicao em nenhum ponto da rolagem.
    - **Bug relatado pelo usuario: selecionar uma linha da tabela (ou esperar a atualizacao automatica de 8s) jogava a rolagem de volta pro topo**, mesmo com o item selecionado continuando destacado. Causa: `renderPayments()` recria o `innerHTML` de `#paymentList` do zero a cada chamada (inclusive nas atualizacoes automaticas via `refreshRemoteState`/`render()`), o que sempre reseta o `scrollTop` do `.catalog-table-wrap` por ser um elemento novo no DOM. Corrigido guardando o `scrollTop` do wrap antigo antes de trocar o `innerHTML` e reaplicando no novo wrap logo em seguida (`app.js`, inicio/fim do bloco que monta `#paymentList`). Testado programaticamente: rolar, forcar nova renderizacao, conferir que a posicao se mantem identica.
    - **Ajuste fino pedido pelo usuario apos o teste real da correcao acima**: a barra de botoes (Fechar/Excluir/Editar) ficou funcionalmente correta mas alta demais no mobile. Reduzido escopado dentro do `@media(max-width:1024px)`: `#paymentDetailDialog .dialog-actions{padding:8px 14px calc(8px+safe-area)}` (era 12px/12px, herdado do padrao geral de `dialog .dialog-actions`) e `#paymentDetailDialog .dialog-actions button{padding:9px 8px}` (era 12px 18px, herdado de `.primary/.secondary/.danger-button`) — altura da barra caiu de 68px pra 54px (medido via Playwright), sem tocar no padrao geral usado pelos outros dialogs nem no desktop deste.
    - **Bug relatado pelo usuario: pagamentos do mesmo dia nao tinham ordem consistente/cronologica** — `renderPayments()` so ordenava por `date` (`b.date.localeCompare(a.date)`), sem desempate; pagamentos com a mesma data ficavam na ordem em que estavam no array `state.payments`, que pode mudar apos sincronizar com o Supabase. Corrigido acrescentando `createdAt` (timestamp completo, ja existente em todo pagamento) como criterio de desempate: `.sort((a,b) => b.date.localeCompare(a.date) || String(b.createdAt||"").localeCompare(String(a.createdAt||"")))`. Mesmo padrao ja usado em outras listas do sistema (ex.: `renderServiceRequests`, linha ~965).
